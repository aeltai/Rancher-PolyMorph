# s3

Pull and push Rancher backup tarballs via S3.

Configure `s3.bucket`, `region`, and optional `prefix` in `rancher-polymorph.yaml`.

## pull

```bash
rancher-polymorph s3 pull migrations/source-full.tar.gz -o ./backups/in.tar.gz
```

## push

```bash
rancher-polymorph s3 push ./backups/sanitized.tar.gz migrations/out.tar.gz
```

## list

```bash
rancher-polymorph s3 list
rancher-polymorph s3 list migrations/
```

Supports custom endpoints (MinIO) via `s3.endpoint` in config.
